<?php
    // Prepare the parameters
    $data = $block->getData('data');
    $title = $block->getData('title');
    $config = $block->getData('config');

    // Get the product id
    $productId = $config['product']['id'];
?>

<!-- UI logs viewer container -->
<div id="aip-ui-logger-<?= $productId ?>" class="messages aip-ui-logger">
    <div class="message success">
        <p><?= $title ?> <?= __('UI Logger') ?></p>
        <div class="message-text">
            <pre class="aip-logger-tree">
                <img class="aip-spinner" src="<?= $config['ui']['loader'] ?>">
            </pre>
        </div>
    </div>
</div>
