<?php 
    // Get the confirmation screen content
    $content = $block->getConfirmContent();
    $addresses = $content['addresses'];
    $shippingRates = $content['shippingRates'];
    $cards = $content['savedCards'];

    // Get the default values
    $paymentToken = $content['sectionData']['paymentToken'];
    $shippingAddress = $content['sectionData']['shippingAddress'];
    $billingAddress = $content['sectionData']['billingAddress'];
?>
<label for="aii-billing-address-select">
    <strong><?= $block->escapeHtml(__('Billing address')) ?>:</strong>
    <p>
        <select id="aii-billing-address-select" class="aii-select js-states form-control">
            <?php foreach ($addresses as $item): ?>
                <option value="<?= $item['entity_id'] ?>" <?= $item['entity_id'] == $billingAddress['id'] ? 'selected' : '' ?>>
                    <?= $item['street'] ?>&nbsp;
                    <?= $item['postcode'] ?>&nbsp;
                    <?= $item['city'] ?>&nbsp;
                    <?= $item['country_id'] ?>
                </option>
            <?php endforeach; ?>
        </select>
    </p>
</label>

<label for="aii-shipping-address-select">
    <strong><?= $block->escapeHtml(__('Shipping address')) ?>:</strong>
    <p>
        <select id="aii-shipping-address-select" class="aii-select js-states form-control">
            <?php foreach ($addresses as $item): ?>
                <option value="<?= $item['entity_id'] ?>" <?= $item['entity_id'] == $shippingAddress['id'] ? 'selected' : '' ?>>
                    <?= $item['street'] ?>&nbsp;
                    <?= $item['postcode'] ?>&nbsp;
                    <?= $item['city'] ?>&nbsp;
                    <?= $item['country_id'] ?>
                </option>
            <?php endforeach; ?>
        </select>
    </p>
</label>

<label for="aii-shipping-method-select">
    <strong><?= $block->escapeHtml(__('Shipping method')) ?>:</strong>
    <p>
        <select id="aii-shipping-method-select" class="aii-select js-states form-control">
            <?php foreach ($shippingRates as $item): ?>
                <option value="<?= $item['value'] ?>">
                    <?= $item['label'] ?>&nbsp;
                    <?= $item['price'] ?>
                </option>
            <?php endforeach; ?>
        </select>
    </p>
</label>

<label for="aii-payment-method-select">
    <strong><?= $block->escapeHtml(__('Payment method')) ?>:</strong>
    <p>
        <select id="aii-payment-method-select" class="aii-select js-states form-control">
            <?php foreach ($cards as $item): ?>
                <option value="<?= $item['data']->getPublicHash() ?>">
                    <img src="<?= $item['icon'] ?>" alt="<?= $item['data']->getType() ?>">&nbsp;
                    <?= $item['data']->getType() ?>&nbsp;
                    <?= $item['data']->getExpiresAt() ?>&nbsp;
                    <?= $item['data']->getPaymentMethodCode() ?>
                </option>
            <?php endforeach; ?>
        </select>
    </p>
</label>