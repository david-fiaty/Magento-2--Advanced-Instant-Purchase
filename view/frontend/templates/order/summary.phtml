<?php
/**
 * Naxero.com
 * Professional ecommerce integrations for Magento.
 *
 * PHP version 7
 *
 * @category  Magento2
 * @package   Naxero
 * @author    Platforms Development Team <contact@naxero.com>
 * @copyright © Naxero.com all rights reserved
 * @license   https://opensource.org/licenses/mit-license.html MIT License
 * @link      https://www.naxero.com
 */
?>

<?php
    // Get the data
    $data = $block->getData('data');
    $params = $block->getData('params');

    $shippingRate = !empty($data['shippingRates'][0]) ? $data['shippingRates'][0]['carrier_price'] : null;
    $productQantity = $block->getData('product_quantity');
    $showDiscountForm = $data['config']['popups']['popup_show_discount_form'];
?>

<div class="nbn-summary-box">
<span class="nbn-label"><?= $block->escapeHtml(__('Order summary')) ?></span>
<table border="0">
  <tbody>
    <tr>
      <td><?= __('Order total') ?></td>
      <td class="nbn-align-right">
        <?= $data['config']['product']['title'] ?>
        <?= $block->productHelper->getPriceBoxHtml($data['config'], $productQantity) ?>
        </td>
    </tr>
  </tbody>
  <tfoot>
    <tr id="nbn-discount-row">
      <td><?= __('Discount') ?></td>
      <td class="nbn-discounted-amount"></td>
    </tr>
    <tr>
     <td><?= __('Shipping') ?></td>
      <td class="nbn-summary-shipping nbn-align-right">
        <?php if ($shippingRate): ?>
        <?= $block->toolsHelper->renderAmount($shippingRate) ?>
        <?php endif; ?>
      </td>
    </tr>
    <tr>
      <td><?= __('Payment total') ?></td>
      <td class="nbn-summary-total nbn-align-right"><?= $block->getTotal($data, $productQantity, $shippingRate) ?></td>
    </tr>
  </tfoot>
</table>
</div>

<?php if ($showDiscountForm): ?>
<?= $block->getCouponBoxHtml($data['config']) ?>
<?php endif; ?>